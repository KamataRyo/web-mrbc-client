'use strict'

describe 'test of directives:', ->
    app = angular.module 'web-mrbc-client'

    describe 'test of file-reader directive:', ->
        it 'should be a spec', ->
            (1).toEqual(1)
